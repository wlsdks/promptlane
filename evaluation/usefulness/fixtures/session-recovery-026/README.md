# Webhook session holdout

Two continuation paths are plausible and Git does not record the previous
session decision. Do not infer it from file order. This fixture is read-only.

- webhook deduplication
- webhook retry scheduling
